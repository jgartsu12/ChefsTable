import React, { Components } from 'react';

export default class PhlogImageList extends Components {
    constructor(props) {
        super(props);

        this.state = {
            displayPhlogEditor: false,
            importToPhlogCarousel: false
        };
    }

    render() {
        const { _id,  }
    }
}