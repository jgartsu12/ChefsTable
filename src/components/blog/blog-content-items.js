import React from 'react';
import { Link } from 'react-router-dom'
import striptags from "striptags";
import Truncate from "react-truncate";

const BlogContentItems = props =>
    const {
        id,
        blog_status,
        blog_description,
        blog_title,
        blog_image_url
    }